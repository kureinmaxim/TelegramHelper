# macOS helpers

This folder is reserved for optional local shortcuts (Tailscale / Headscale / VLESS).

Do not commit machine-specific IPs, SSH ports, or login-server hostnames.
Point scripts at placeholders such as `YOUR_HEADSCALE_URL` and `YOUR_VPS_IP`.
