# Python 3.11+
python-telegram-bot>=22.1,<23.0
qrcode[pil]
deep-translator>=1.11.0
requests>=2.31.0
langid>=1.1.6
beautifulsoup4>=4.12.0
openai>=1.16.0
anthropic>=0.25.0
httpx>=0.27.0,<0.28.0
python-dotenv>=1.0.0
aiofiles>=23.0.0

# SSH CLI dashboard (cli_dashboard.py) — terminal tables/panels
rich>=13.0.0
# Command autocomplete (TAB) + searchable history (Ctrl+R) in the CLI dashboard
prompt_toolkit>=3.0.0

# Optional: send via Gmail API (GMAIL_OAUTH_CREDENTIALS)
google-api-python-client>=2.140.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Resilient search provider for /factcheck
ddgs>=1.0.0

# API Server
fastapi>=0.109.0
uvicorn>=0.27.0
cryptography>=42.0.0 